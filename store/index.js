export const state = () => ({});

export const mutations = {
  // mutation
};

export const actions = {
  // Actions
};

export const getters = {
  // getters
  loggedIn(state) {
    return state.teachers.loggedIn;
  }
};
